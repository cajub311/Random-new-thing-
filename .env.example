# OpenClaw — all keys optional.
#
# Default: free keyless Pollinations. Set provider keys below to unlock
# faster models (Groq, Gemini, etc.) on the server, or paste keys in the UI.

# ── Cloud providers (keys optional unless you use that provider) ─────────
GROQ_API_KEY=
GEMINI_API_KEY=
COHERE_API_KEY=
TOGETHER_API_KEY=
HF_API_KEY=

# ── Optional messaging adapters ────────────────────────────────────────
# Set TELEGRAM_BOT_TOKEN to run the Telegram bot (long-polling, no webhook
# config needed). Create a bot via @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=

# ── Operational ────────────────────────────────────────────────────────
PORT=3000
LOG_LEVEL=info        # debug | info | warn | error
LOG_FORMAT=pretty     # pretty | json
WORKSPACE_DIR=        # default: ./workspace (serverless fallback to /tmp)
